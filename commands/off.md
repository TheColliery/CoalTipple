---
description: Turn CoalTipple routing OFF for this session — work as a normal single agent
---

Turn CoalTipple routing OFF for the rest of this session: do not delegate-down or escalate-up — work as a normal single agent. Confirm in one line, in the user's language.

This is the `/coaltipple off` action exposed as a discoverable command. To persist the setting across every session, set `enableRouting: false` in `.coaltipple.json` instead.
