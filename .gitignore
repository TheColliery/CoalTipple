# Internal governance + design notes — machine-local, never shipped to the public repo
CLAUDE.md
MEMORY*.md
AGENTS.md
.claude/
.agents/

# IP-sensitive design + roadmap — stays local until the series is ready for it
# (DESIGN = full vision incl. unbuilt 5-tier/Haldane/CoalHearth; SPEC = v1 build contract)
COALTIPPLE_DESIGN.md

# Board #44: the org's -p resident-dispatch layer, a proposal for what CT could ship
# so a department head grades a `-p` dispatch the same way CT grades an Agent-tool
# spawn -- describes something that does not exist as a shipped contract yet.
COALTIPPLE_RESIDENT_DISPATCH_DESIGN.md

# Internal dogfood / validation session artifacts — methodology + raw routing results
# + an internal fix-queue; NOT part of the shipped product (un-ignore if you later
# want a polished public eval harness, the way CoalMine ships eval/).
dogfood/

# Generated SkillSpector scan reports (provenance recorded in SECURITY.md; the raw report is not shipped)
skillspector-*


# CC platform-generated skill lockfile (machine-local, never ship)
skills-lock.json
